Python 2.7

Плюсом первой является, то что её буфер способен работать с различными типами данных (любые числа, строки, списки и пр.).
    В то время как вторая ограничивается набором из 12 числовых типов + 1 символьный тип (unicod character)

Вторая реализация быстрее и тратит меньше ресурсов, так как работает с массивами, а не со списками.
    Массивы хранят данные более компактно и эфективно
    Также array поддерживает математические операции
